# ============================================================
# Open Family Cloud — Override 設定例（パターン1: VPS + Docker Compose）
#
# このファイルを docker-compose.override.yml にコピーして
# 家庭ごとのカスタマイズを記述してください。
#
#   cp docker-compose.override.example.yml docker-compose.override.yml
# ============================================================

services:

  # ----------------------------------------------------------
  # 例1: Jellyfin で GPU トランスコードを有効化（Intel QSV）
  # ----------------------------------------------------------
  # jellyfin:
  #   devices:
  #     - /dev/dri:/dev/dri
  #   environment:
  #     - JELLYFIN_FFmpeg__hardwareAccelerationType=vaapi

  # ----------------------------------------------------------
  # 例2: Nextcloud の PHP メモリ上限を引き上げ
  # ----------------------------------------------------------
  # nextcloud:
  #   environment:
  #     - PHP_MEMORY_LIMIT=1024M
  #     - PHP_UPLOAD_LIMIT=16G

  # ----------------------------------------------------------
  # 例3: 不要なサービスを無効化
  # ----------------------------------------------------------
  # jellyfin:
  #   profiles:
  #     - disabled
  # jitsi-web:
  #   profiles:
  #     - disabled
  # jitsi-prosody:
  #   profiles:
  #     - disabled
  # jitsi-jicofo:
  #   profiles:
  #     - disabled
  # jitsi-jvb:
  #   profiles:
  #     - disabled

  # ----------------------------------------------------------
  # 例4: PostgreSQL のリソース制限
  # ----------------------------------------------------------
  # postgres:
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 2G
  #       reservations:
  #         memory: 512M
