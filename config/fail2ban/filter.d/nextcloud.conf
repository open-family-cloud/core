# Nextcloud — ログイン失敗検出フィルタ
# Nextcloud の JSON ログ形式に対応

[Definition]
failregex = ^.*"remoteAddr":"<HOST>".*"message":"Login failed:.*$
            ^.*"remoteAddr":"<HOST>".*"message":"Trusted domain error.*$

ignoreregex =
